<!DOCTYPE html>
<html>
  <head>
    <style>
       /* Set the size of the div element that contains the map */
      #map {
        height: 400px;  /* The height is 400 pixels */
        width: 100%;  /* The width is the width of the web page */
       }
    </style>
  </head>
  <body>
    <h3>My Google Maps Demo</h3>
    <!--The div element for the map -->
    <div id="map"></div>
    <script>
// Initialize and add the map

var branchmap = L.map('branches').setView([39.7507834, -104.9986242], 11);

function initMap() {
  // The location of Uluru
  var denver = {lat: 39.7507834, lng: -104.9986242};
  // The map, centered at Danver
  var map = new google.maps.Map(
      document.getElementById('map'), {zoom: 4, center: denver});
  // The marker, positioned at denver
  var marker = new google.maps.Marker({position: denver, map: map});
}
    </script>
    <!--Load the API from the specified URL
    * The async attribute allows the browser to render the page while the API loads
    * The key parameter will contain your own API key (which is not needed for this tutorial)
    * The callback parameter executes the initMap() function
    -->
    <script>
    let dotenv = require('dotenv').config();
    let gmap_key = process.env.GMAP_key;
    async defer
    

    src="https://maps.googleapis.com/maps/api/js?" + gmap_key +"&callback=initMap"
    </script>
  </body>
</html>